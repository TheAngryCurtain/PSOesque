using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[System.Serializable]
public class Inventory
{
    // will need to store all items
    // will need to store equipped items? that can probably be derived from item data on a case by case? (TODO equipment slot enum?)
}
